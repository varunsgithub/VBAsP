#Choice of ownership structure:
 Since Rust enforces borrower checker rules on variables, I have decided to go with creating 
 one single struct which owns all individual components and the components borrow and implement
 the system process amongst themselves.